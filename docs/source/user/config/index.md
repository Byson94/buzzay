# Configuration

Buzzay is configured in TOML, a simple configuration language. The configuration file should be located at 
`$XDG_CONFIG_HOME/buzzay/config.toml`. On most systems, this will be the `~/.config/buzzay/config.toml` file.

```{toctree}
core
env
eyecandy/index
bindings
example
```

